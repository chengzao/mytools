{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf ./output && mkdir output",
    "prebuild": "echo I run before the build script && npm run clean",
    "parcel": "parcel index.html -p 8800",
    "build": "parcel build index.html --out-dir output --public-url ./",
    "postbuild": "echo I run after the build script"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^7.2.5",
    "babel-core": "^6.26.3",
    "modules": "^0.4.0",
    "posthtml-img-autosize": "^0.1.1"
  },
  "dependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "node-sass": "^4.7.2",
    "parcel-bundler": "^1.2.0",
    "parcel-plugin-vue": "^1.3.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "typescript": "^2.6.2",
    "vue": "^2.5.11"
  }
}
